import Chip from "../../../../components/chip";
import { TableProps } from "../../../../components/table/types";
import './rigntSection.sass'

export const infoTableCreateModules = {
    dataHead: [
        { id: "name", label: "Nombre" },
        { id: "create", label: "Creado" }
    ]
}